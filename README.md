# Healthcare-ai
Drug prescription --> Using video generation in Human body. 
create a .env file
GOOGLE_API_KEY = ""

```
uv venv --python 3.12
uv pip install google-genai pandas dotenv json
bash b.sh
python b.py --root_dir ./runs
```
